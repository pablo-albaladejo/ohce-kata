const reverseStr = (str) => {
    //TODO a function that reverse the string
    return null;
}

const isBetween = (num, range) => {
    //TODO a function that decide if certain hour is withn certain time range
    return null;
}

const isPalindrome = (str) => {
    //TODO a function that return if a tring is palindrome
    return null;
}

module.exports = {
    reverseStr,
    isBetween,
    isPalindrome
}
