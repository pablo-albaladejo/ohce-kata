const greet = (name, hours) => {
    //TODO according to hour, it should greet user with good morning, good afternoon or good night
    //TODO need a function to decide moring, afternoon or night according to the current hour

    return null;
}

const goodbye = (name) => `Goodbye ${name}!`

module.exports = {
    greet,
    goodbye
}
